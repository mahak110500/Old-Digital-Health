// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  apiUrl: "https://jsonplaceholder.typicode.com",
  baseUrl: "http://95.111.202.157:4000/",
  default_taxonomy_general: 1,
  default_taxonomy_digital: 6,
  default_development_id: 1,
  default_ultimate_id: 2,
  default_developments: "1,2",
  default_governances: "1,2",
  default_country_2021:"74,228",
  default_country_2022:"106,108",
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
